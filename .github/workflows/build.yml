name: Build OutlookEmailExtractor EXE

on:
  workflow_dispatch: {}
  push:
    tags:
      - "v*"

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt pyinstaller

      # IMPORTANT: single command, run under bash, point to the exact file path
      - name: Build EXE with PyInstaller
        shell: bash
        run: |
          ls -la
          echo "PWD: $(pwd)"
          pyinstaller --noconfirm --onefile --windowed \
            --name OutlookEmailExtractor \
            --hidden-import win32timezone \
            --hidden-import win32com \
            --hidden-import pythoncom \
            ./OutlookEmailExtractor.py

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: OutlookEmailExtractor-win
          path: dist/OutlookEmailExtractor.exe

  # Optional: attach the EXE to a GitHub Release when you push a tag like v1.0.0
  release:
    needs: build-windows
    if: startsWith(github.ref, 'refs/tags/')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: OutlookEmailExtractor-win
          path: ./release
      - uses: softprops/action-gh-release@v2
        with:
          files: ./release/OutlookEmailExtractor.exe
